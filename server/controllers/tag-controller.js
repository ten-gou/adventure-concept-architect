const { Tag } = require('../models');

module.exports = {
    // get a single tag by id or tagTitle

    // get all tags

    // create a tag

    // update a tag by id or tagTitle

    // delete a tag by id or tagTitle

}